# 手动部署指南 - GitHub Pages

由于无法通过 Cursor 登录 GitHub，请按照以下步骤手动上传文件。

## 📦 需要上传的文件清单

### ✅ 必须上传的文件（核心文件）

```
AI 导购 渠道质量评判 PSM DID uplift/
│
├── app/                                    # ✅ 必须上传整个目录
│   ├── layout.tsx
│   ├── page.tsx
│   ├── page.css
│   └── globals.css
│
├── components/                             # ✅ 必须上传整个目录
│   ├── Dashboard.tsx
│   ├── FunnelTable.tsx
│   ├── UpliftTable.tsx
│   └── DIDChart.tsx
│
├── public/                                 # ✅ 必须上传整个目录
│   ├── funnel_by_channel.json
│   ├── uplift_by_channel.json
│   └── did_timeseries.json
│
├── .github/                                # ✅ 必须上传（GitHub Actions 配置）
│   └── workflows/
│       └── deploy.yml
│
├── package.json                            # ✅ 必须上传
├── tsconfig.json                           # ✅ 必须上传
├── next.config.js                          # ✅ 必须上传
├── .gitignore                              # ✅ 必须上传
│
└── README.md                               # ✅ 建议上传
```

### ❌ 不需要上传的文件

```
node_modules/          # ❌ 不需要（会自动安装）
.next/                 # ❌ 不需要（会自动构建）
out/                   # ❌ 不需要（会自动生成）
*.csv                  # ❌ 不需要（数据已转为 JSON）
*.py                   # ❌ 不需要（Python 脚本）
*.md（除了 README.md） # ❌ 可选（文档文件）
```

## 🚀 方法一：通过 GitHub Web 界面上传（最简单）

### 步骤 1：准备文件

1. 在本地创建一个新文件夹，命名为 `PSM-DID-uplift`
2. 将以下文件夹和文件复制到这个新文件夹：
   - `app/` 文件夹（整个文件夹）
   - `components/` 文件夹（整个文件夹）
   - `public/` 文件夹（整个文件夹）
   - `.github/` 文件夹（整个文件夹）
   - `package.json`
   - `tsconfig.json`
   - `next.config.js`
   - `.gitignore`
   - `README.md`

### 步骤 2：上传到 GitHub

1. 访问：https://github.com/MyraWang0406/PSM-DID-uplift
2. 点击 "uploading an existing file" 或 "Add file" → "Upload files"
3. 将准备好的文件夹拖拽到上传区域
4. 在页面底部填写提交信息：
   ```
   Initial commit: AI导购效果分析看板
   ```
5. 选择 "Commit directly to the main branch"
6. 点击 "Commit changes"

### 步骤 3：启用 GitHub Pages

1. 在仓库页面，点击右上角的 **Settings**
2. 在左侧菜单找到 **Pages**
3. 在 "Source" 部分，选择 **"GitHub Actions"**
4. 保存设置

### 步骤 4：等待自动部署

1. 点击仓库顶部的 **Actions** 标签
2. 等待 "Deploy to GitHub Pages" 工作流完成（通常需要 2-5 分钟）
3. 部署成功后，访问：https://myrawang0406.github.io/PSM-DID-uplift/

## 🗂️ 方法二：使用 GitHub Desktop（如果有安装）

1. 打开 GitHub Desktop
2. File → Add Local Repository
3. 选择项目文件夹
4. 点击 Publish repository
5. 选择仓库：MyraWang0406/PSM-DID-uplift
6. 点击 Publish

## 📋 最小文件清单（如果空间有限）

如果只能上传最少的文件，至少需要这些：

```
app/
  ├── layout.tsx
  ├── page.tsx
  ├── page.css
  └── globals.css

components/
  ├── Dashboard.tsx
  ├── FunnelTable.tsx
  ├── UpliftTable.tsx
  └── DIDChart.tsx

public/
  ├── funnel_by_channel.json
  ├── uplift_by_channel.json
  └── did_timeseries.json

.github/
  └── workflows/
      └── deploy.yml

package.json
tsconfig.json
next.config.js
.gitignore
README.md
```

## ⚠️ 注意事项

1. **文件夹结构**：必须保持完整的文件夹结构
2. **隐藏文件**：`.github` 和 `.gitignore` 是隐藏文件，确保上传时包含它们
3. **文件名大小写**：注意文件名的大小写，必须完全一致
4. **JSON 文件**：确保 `public/` 目录下的三个 JSON 文件都在

## 🔍 验证上传是否成功

上传后，检查仓库是否包含：
- ✅ `app/` 文件夹（包含 4 个文件）
- ✅ `components/` 文件夹（包含 4 个文件）
- ✅ `public/` 文件夹（包含 3 个 JSON 文件）
- ✅ `.github/workflows/deploy.yml` 文件
- ✅ `package.json` 文件

## 🐛 如果部署失败

1. 检查 Actions 标签页的错误信息
2. 确保所有必需文件都已上传
3. 检查 `package.json` 中的依赖是否正确
4. 确保 GitHub Pages 设置中选择的是 "GitHub Actions"

## 📞 需要帮助？

如果遇到问题，可以：
1. 检查 GitHub Actions 的日志
2. 确认所有文件都已正确上传
3. 验证文件夹结构是否正确


