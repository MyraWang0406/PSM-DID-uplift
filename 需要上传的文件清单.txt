===========================================
GitHub 部署 - 需要上传的文件清单
===========================================

⚠️ 重要：请按照以下清单，将文件拖拽上传到 GitHub

📁 文件夹结构（必须保持完整）：

1. app/ 文件夹（整个文件夹）
   ├── layout.tsx
   ├── page.tsx
   ├── page.css
   └── globals.css

2. components/ 文件夹（整个文件夹）
   ├── Dashboard.tsx
   ├── FunnelTable.tsx
   ├── UpliftTable.tsx
   └── DIDChart.tsx

3. public/ 文件夹（整个文件夹）
   ├── funnel_by_channel.json
   ├── uplift_by_channel.json
   └── did_timeseries.json

4. .github/ 文件夹（整个文件夹，注意前面有点）
   └── workflows/
       └── deploy.yml

📄 根目录文件（单独上传）：

5. package.json
6. tsconfig.json
7. next.config.js
8. .gitignore（注意前面有点）
9. README.md（可选，但建议上传）

===========================================
❌ 不需要上传的文件：
===========================================

- node_modules/ （会自动安装）
- .next/ （会自动构建）
- out/ （会自动生成）
- *.csv 文件
- *.py 文件
- 其他 .md 文档文件（可选）

===========================================
🚀 上传步骤：
===========================================

1. 访问：https://github.com/MyraWang0406/PSM-DID-uplift

2. 点击 "Add file" → "Upload files"

3. 将上述文件夹和文件拖拽到上传区域
   （注意：可以一次性拖拽整个文件夹）

4. 在页面底部填写：
   提交信息：Initial commit: AI导购效果分析看板
   
5. 选择 "Commit directly to the main branch"

6. 点击 "Commit changes"

7. 等待上传完成

8. 进入 Settings → Pages → Source 选择 "GitHub Actions"

9. 等待 Actions 自动部署（约 2-5 分钟）

10. 访问：https://myrawang0406.github.io/PSM-DID-uplift/

===========================================
✅ 验证清单：
===========================================

上传后，检查仓库是否包含：
□ app/ 文件夹（4个文件）
□ components/ 文件夹（4个文件）
□ public/ 文件夹（3个JSON文件）
□ .github/workflows/deploy.yml
□ package.json
□ tsconfig.json
□ next.config.js
□ .gitignore

===========================================

