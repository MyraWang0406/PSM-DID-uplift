# 快速启动指南

## 📦 项目结构

```
AI 导购 渠道质量评判 PSM DID uplift/
├── app/                          # Next.js App Router
│   ├── layout.tsx                # 根布局
│   ├── page.tsx                  # 首页（Dashboard入口）
│   └── globals.css               # 全局样式
├── components/                   # React 组件
│   ├── Dashboard.tsx            # Dashboard 主组件
│   ├── FunnelTable.tsx           # 渠道漏斗表
│   ├── UpliftTable.tsx           # PSM Uplift 表
│   └── DIDChart.tsx              # DID 折线图
├── public/                       # 静态资源
│   ├── funnel_by_channel.json    # 渠道漏斗数据
│   ├── uplift_by_channel.json    # PSM Uplift 数据
│   └── did_timeseries.json       # DID 时间序列数据
├── package.json                  # 项目依赖配置
├── tsconfig.json                 # TypeScript 配置
├── next.config.js                # Next.js 配置
└── README.md                     # 项目说明
```

## 🚀 启动步骤

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

### 3. 访问应用

打开浏览器访问：**http://localhost:3000**

## 📊 功能模块

### 1. 渠道漏斗表
- 展示各渠道的会话数、询单数、下单数
- 计算询单率、下单率、转化率
- 按会话数排序

### 2. PSM Uplift 表
- 展示各渠道的 Lead 和 Order 提升率
- 显示样本量和可信度标签（高/中/低）
- 按可信度和提升率排序
- 颜色区分正向/负向效果

### 3. DID 折线图
- 展示 AI On/Off 组的 Lead 率时间序列对比
- 展示 AI On/Off 组的 Order 率时间序列对比
- 标注活动周（第30-36天）

## 🎨 设计特点

- ✅ SaaS 风格（浅色主题）
- ✅ 卡片式布局
- ✅ 清晰的表格展示
- ✅ 标签（Tag）用于可信度标识
- ✅ 响应式设计

## 🔧 开发命令

```bash
# 开发模式
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm start

# 代码检查
npm run lint
```

## 📝 数据更新

如果需要更新数据文件，运行：

```bash
python generate_json_data.py
```

这会从 CSV 文件重新生成 JSON 数据文件到 `public/` 目录。

## 🐛 常见问题

### 端口被占用
如果 3000 端口被占用，Next.js 会自动使用下一个可用端口（如 3001）。

### 数据加载失败
确保 `public/` 目录下有三个 JSON 文件：
- `funnel_by_channel.json`
- `uplift_by_channel.json`
- `did_timeseries.json`

### TypeScript 错误
运行 `npm install` 确保所有依赖已正确安装。

## 📚 技术栈

- **Next.js 14** - React 框架（App Router）
- **React 18** - UI 库
- **TypeScript** - 类型安全
- **Recharts** - 图表库


